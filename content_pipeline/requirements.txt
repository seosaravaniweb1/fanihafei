# همه‌ی وابستگی‌ها رایگان‌اند. هیچ سرویس پولی در این پروژه استفاده نمی‌شود.

# --- هسته (بدون این‌ها ابزار اجرا نمی‌شود) -----------------------------------
typer>=0.12
PyYAML>=6.0

# --- شبکه و پارس -------------------------------------------------------------
curl_cffi>=0.7            # impersonate="chrome" — جایگزین requests خام
selectolax>=0.3           # پارس سریع HTML
rapidfuzz>=3.9            # شباهت متنی

# --- تشخیص موضوعی (لوکال و رایگان) -------------------------------------------
sentence-transformers>=3.0    # intfloat/multilingual-e5-large

# --- نرمال‌سازی فارسی (اختیاری؛ توابع سفارشی مستقل کار می‌کنند) ---------------
hazm>=0.10

# --- خروجی -------------------------------------------------------------------
openpyxl>=3.1             # اکسل
gspread>=6.0              # گوگل‌شیت (اختیاری)

# --- fallback مرورگر برای سایت‌های JS-heavy ----------------------------------
playwright>=1.45          # پس از نصب: playwright install chromium

# --- تست ---------------------------------------------------------------------
pytest>=8.0

# استفاده نشود:
#   cloudscraper (متروک)، newspaper3k (بدون نگهداری)،
#   requests خام (اثرانگشت TLS آن شناسایی می‌شود).
