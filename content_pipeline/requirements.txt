# --- هسته (بدون این‌ها ابزار اجرا نمی‌شود) -----------------------------------
typer>=0.12
PyYAML>=6.0

# --- شبکه و پارس (به‌شدت توصیه‌شده) ------------------------------------------
curl_cffi>=0.7            # عبور از Cloudflare با impersonate="chrome"
selectolax>=0.3           # پارس سریع HTML
trafilatura>=1.12         # استخراج متن اصلی (حذف هدر/فوتر/سایدبار)
rapidfuzz>=3.9            # شباهت متنی سریع

# --- کیفیت تشخیص موضوعی (اختیاری ولی مؤثر) ----------------------------------
sentence-transformers>=3.0    # intfloat/multilingual-e5-large

# --- سرویس‌ها و خروجی --------------------------------------------------------
anthropic>=0.40           # داوری ادغام، بنچمارک کیفی و Vision
gspread>=6.0              # نوشتن در گوگل‌شیت
openpyxl>=3.1             # خروجی xlsx
Pillow>=10.0              # چک ابعاد تصویر

# --- fallback مرورگر برای سایت‌های JS-heavy ----------------------------------
playwright>=1.45          # پس از نصب: playwright install chromium

# --- تست ---------------------------------------------------------------------
pytest>=8.0

# نکته: این بسته‌ها عمداً استفاده نشده‌اند —
#   cloudscraper (متروک)، newspaper3k (بدون نگهداری)،
#   puppeteer-extra-stealth (شناسایی می‌شود)،
#   Google Custom Search JSON API (برای کاربران جدید بسته است).
